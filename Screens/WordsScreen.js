import React from 'react';
import {Text} from "react-native";

const WordsScreen = () => {
    return (
        <Text style={{marginTop:30}}>
        This is words screen
        </Text>
    );
};

export default WordsScreen;